package game.duke;

/**
 * Type of a duke.
 */
public enum DukeType {
	PLAYER,                                 /** Duke is a player. */
	BARON;                                  /** Duke is a non-player (baron). */
}
